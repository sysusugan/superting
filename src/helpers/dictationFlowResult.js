import core from "./dictationFlowResultCore.cjs";

export const normalizeDictationResult = core.normalizeDictationResult;
export const normalizeTranscriptionResult = core.normalizeTranscriptionResult;
export const normalizeMeetingSegment = core.normalizeMeetingSegment;
export const resolveStreamingDictationText = core.resolveStreamingDictationText;
export const settleStreamingStop = core.settleStreamingStop;
export const pickDictationWarning = core.pickDictationWarning;
